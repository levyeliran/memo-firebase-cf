{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "devDependencies": {
    "typescript": "^2.3.2"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "deploy": "tsc && firebase deploy --only functions:funcGroup"
  },
  "private": true
}
